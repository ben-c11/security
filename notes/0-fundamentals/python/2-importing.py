#!/bin/python3

#5.09.40 	Importing

#Use import keyword;
import sys #system functions and parameters
from datetime import datetime as dt #import just the datetime from the library also called datetime; as = import as an alias, doesn't need to be called datetime, we can make it known as dt.

print(sys.version) #does not work! Need to import sys - used a lot
print(dt.now())


#now move to s.py :) 5.15.35 
